# Deployment TODO (GitHub Pages)

- [ ] Create/confirm Git repo status (if not already initialized) and verify files are in repo root.
- [ ] Add and commit: index.html, style.css, first.js (and images/ if present).
- [ ] Push to GitHub remote on `main` branch.
- [ ] Enable GitHub Pages in the repository settings (Pages → Deploy from branch → main → /root).
- [ ] Wait for Pages to deploy and open the generated URL to verify it works.

